# 檔案結構更新說明

## 變更摘要

所有頁面相關的檔案已經移動到 `pages` 資料夾中，以提供更好的專案組織結構。

## 檔案移動清單

### 從根目錄移至 pages/

- `login.html` → `pages/login.html`
- `index.html` → `pages/index.html`

### 從根目錄子資料夾移至 pages/

- `styles/login.css` → `pages/styles/login.css`
- `scripts/login.js` → `pages/scripts/login.js`

### 刪除的空資料夾

- `styles/` (根目錄)
- `scripts/` (根目錄)

## 更新的檔案

### 1. main.js

**變更內容：**

- 更新登入頁面載入路徑：`"login.html"` → `"pages/login.html"`
- 更新主頁面載入路徑：`"index.html"` → `"pages/index.html"`

```javascript
// 之前
mainWindow.loadFile("login.html");
mainWindow.loadFile("index.html");

// 現在
mainWindow.loadFile("pages/login.html");
mainWindow.loadFile("pages/index.html");
```

### 2. pages/login.html

**變更內容：**

- 更新 CSS 檔案路徑：`"styles/login.css"` → `"./styles/login.css"`
- 更新 JS 檔案路徑：`"scripts/login.js"` → `"./scripts/login.js"`

```html
<!-- 之前 -->
<link rel="stylesheet" href="styles/login.css" />
<script src="scripts/login.js"></script>

<!-- 現在 -->
<link rel="stylesheet" href="./styles/login.css" />
<script src="./scripts/login.js"></script>
```

### 3. README.md

**變更內容：**

- 更新專案結構說明，反映新的檔案組織

## 新的專案結構

```
video-assistant/
├── main.js                 # Electron 主程序
├── pages/                  # 📁 所有頁面檔案
│   ├── login.html         # 登入/註冊頁面
│   ├── index.html         # 主應用程式頁面
│   ├── styles/            # 頁面樣式
│   │   └── login.css      # 登入頁面樣式
│   └── scripts/           # 頁面腳本
│       └── login.js       # 登入頁面邏輯
├── config.example.js      # OAuth 設定範例
├── README.md              # 專案說明文件
├── package.json           # 專案配置
└── node_modules/          # 相依套件
```

## 優點

### 1. 更清晰的結構

- 所有頁面相關檔案集中在一個資料夾
- 易於找到和管理頁面檔案

### 2. 更好的擴展性

- 未來新增頁面時，只需在 `pages/` 下建立新的檔案
- 每個頁面可以有自己的子資料夾（如 `pages/login/`, `pages/dashboard/` 等）

### 3. 符合最佳實踐

- 前端專案常見的資料夾結構
- 便於團隊協作和程式碼維護

### 4. 模組化

- 頁面、樣式、腳本分別組織
- 未來可以考慮為每個頁面建立獨立的資料夾

## 未來建議結構（進階）

如果專案持續成長，可以考慮進一步模組化：

```
pages/
├── login/
│   ├── index.html
│   ├── style.css
│   └── script.js
├── main/
│   ├── index.html
│   └── style.css
├── dashboard/
│   ├── index.html
│   ├── style.css
│   └── script.js
└── shared/
    ├── styles/
    │   └── common.css
    └── scripts/
        └── utils.js
```

## 測試確認

應用程式已重新啟動並測試：

- ✅ 登入頁面正常載入
- ✅ CSS 樣式正確應用
- ✅ JavaScript 功能正常運作
- ✅ 登入後可正常切換到主頁面

## 注意事項

1. **路徑引用**：HTML 檔案中使用相對路徑 `./` 來引用同層級的資源
2. **主程序**：main.js 中使用從專案根目錄開始的路徑 `pages/`
3. **相容性**：所有現有功能保持不變，只是檔案位置改變

## 總結

這次檔案重組讓專案結構更加清晰和專業，為未來的擴展打下良好的基礎。所有功能保持正常運作，無需額外的配置。

---

**更新日期**: 2025-10-20  
**版本**: 2.0.0  
**狀態**: ✅ 完成並測試通過
